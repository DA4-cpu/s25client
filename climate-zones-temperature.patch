diff --git a/data/RTTR/gamedata/world/greenland.lua b/data/RTTR/gamedata/world/greenland.lua
index 955f176..4f79aa4 100644
--- a/data/RTTR/gamedata/world/greenland.lua
+++ b/data/RTTR/gamedata/world/greenland.lua
@@ -127,6 +127,7 @@ rttr:AddTerrain{
 	kind = "land",
 	property = "walkable",
 	humidity = 0,
+	temperature = 90,
 	texture = texFile,
 	pos = {48, 0, 32, 31},
 	color = 0xFFc09c7c
@@ -139,6 +140,7 @@ rttr:AddTerrain{
 	edgePriority = 10,
 	kind = "land",
 	property = "unwalkable",
+	temperature = 60,
 	texture = texFile,
 	pos = {96, 0, 32, 31},
 	color = 0xFF649014
@@ -150,6 +152,7 @@ rttr:AddTerrain{
 	s2Id = 0xF,
 	edgePriority = 35,
 	kind = "land",
+	temperature = 50,
 	texture = texFile,
 	pos = {144, 0, 32, 31},
 	color = 0xFF48780c
@@ -206,6 +209,7 @@ rttr:AddTerrain{
 	edgePriority = 15,
 	kind = "land",
 	humidity = 60,
+	temperature = 80,
 	texture = texFile,
 	pos = {0, 96, 32, 31},
 	color = 0xFF649014
@@ -218,6 +222,7 @@ rttr:AddTerrain{
 	edgePriority = 30,
 	kind = "land",
 	humidity = 95,
+	temperature = 45,
 	texture = texFile,
 	pos = {48, 96, 32, 31},
 	color = 0xFF48780c
@@ -229,6 +234,7 @@ rttr:AddTerrain{
 	s2Id = 9,
 	edgePriority = 25,
 	kind = "land",
+	temperature = 50,
 	texture = texFile,
 	pos = {96, 96, 32, 31},
 	color = 0xFF649014
@@ -240,6 +246,7 @@ rttr:AddTerrain{
 	s2Id = 0xA,
 	edgePriority = 20,
 	kind = "land",
+	temperature = 55,
 	texture = texFile,
 	pos = {144, 96, 32, 31},
 	color = 0xFF407008
@@ -252,6 +259,7 @@ rttr:AddTerrain{
 	edgePriority = 60,
 	kind = "land",
 	humidity = 30,
+	temperature = 35,
 	texture = texFile,
 	pos = {0, 144, 32, 31},
 	color = 0xFF88b028
@@ -379,6 +387,7 @@ rttr:AddTerrain{
 	kind = "land",
 	property = "walkable",
 	humidity = 0,
+	temperature = 90,
 	texture = texFile,
 	pos = {48, 0, 32, 31},
 	color = 0xFFc09c7c
diff --git a/data/RTTR/gamedata/world/wasteland.lua b/data/RTTR/gamedata/world/wasteland.lua
index 3e809d4..0918d7b 100644
--- a/data/RTTR/gamedata/world/wasteland.lua
+++ b/data/RTTR/gamedata/world/wasteland.lua
@@ -104,6 +104,7 @@ rttr:AddTerrain{
 	kind = "land",
 	property = "walkable",
 	humidity = 0,
+	temperature = 85,
 	texture = texFile,
 	pos = {48, 0, 32, 31},
 	color = 0xFF9c7c64
@@ -127,6 +128,7 @@ rttr:AddTerrain{
 	s2Id = 0x0F,
 	edgePriority = 40,
 	kind = "land",
+	temperature = 55,
 	texture = texFile,
 	pos = {144, 0, 32, 31},
 	color = 0xFF444850
@@ -183,6 +185,7 @@ rttr:AddTerrain{
 	edgePriority = 40,
 	kind = "land",
 	humidity = 60,
+	temperature = 75,
 	texture = texFile,
 	pos = {0, 96, 32, 31},
 	color = 0xFF444850
@@ -195,6 +198,7 @@ rttr:AddTerrain{
 	edgePriority = 40,
 	kind = "land",
 	humidity = 95,
+	temperature = 50,
 	texture = texFile,
 	pos = {48, 96, 32, 31},
 	color = 0xFF5c5840
@@ -206,6 +210,7 @@ rttr:AddTerrain{
 	s2Id = 0x09,
 	edgePriority = 40,
 	kind = "land",
+	temperature = 55,
 	texture = texFile,
 	pos = {96, 96, 32, 31},
 	color = 0xFF646048
@@ -217,6 +222,7 @@ rttr:AddTerrain{
 	s2Id = 0x0A,
 	edgePriority = 40,
 	kind = "land",
+	temperature = 60,
 	texture = texFile,
 	pos = {144, 96, 32, 31},
 	color = 0xFF646048
@@ -229,6 +235,7 @@ rttr:AddTerrain{
 	edgePriority = 60,
 	kind = "land",
 	humidity = 30,
+	temperature = 80,
 	texture = texFile,
 	pos = {0, 144, 32, 31},
 	color = 0xFF88b028
@@ -359,6 +366,7 @@ rttr:AddTerrain{
 	kind = "land",
 	property = "walkable",
 	humidity = 0,
+	temperature = 85,
 	texture = texFile,
 	pos = {48, 0, 32, 31},
 	color = 0xFF9c7c64
diff --git a/data/RTTR/gamedata/world/winterworld.lua b/data/RTTR/gamedata/world/winterworld.lua
index f219416..4f2fc10 100644
--- a/data/RTTR/gamedata/world/winterworld.lua
+++ b/data/RTTR/gamedata/world/winterworld.lua
@@ -110,6 +110,7 @@ rttr:AddTerrain{
 	kind = "land",
 	property = "walkable",
 	humidity = 0,
+	temperature = 5,
 	texture = texFile,
 	pos = {48, 0, 32, 31},
 	color = 0xFF0070b0
@@ -133,6 +134,7 @@ rttr:AddTerrain{
 	s2Id = 0xF,
 	edgePriority = 13,
 	kind = "land",
+	temperature = 25,
 	texture = texFile,
 	pos = {144, 0, 32, 31},
 	color = 0xFF7c84ac
@@ -189,6 +191,7 @@ rttr:AddTerrain{
 	edgePriority = 18,
 	kind = "land",
 	humidity = 60,
+	temperature = 30,
 	texture = texFile,
 	pos = {0, 96, 32, 31},
 	color = 0xFFa0accc
@@ -201,6 +204,7 @@ rttr:AddTerrain{
 	edgePriority = 23,
 	kind = "land",
 	humidity = 95,
+	temperature = 15,
 	texture = texFile,
 	pos = {48, 96, 32, 31},
 	color = 0xFFb0a494
@@ -212,6 +216,7 @@ rttr:AddTerrain{
 	s2Id = 9,
 	edgePriority = 28,
 	kind = "land",
+	temperature = 20,
 	texture = texFile,
 	pos = {96, 96, 32, 31},
 	color = 0xFF88a874
@@ -223,6 +228,7 @@ rttr:AddTerrain{
 	s2Id = 0xA,
 	edgePriority = 33,
 	kind = "land",
+	temperature = 20,
 	texture = texFile,
 	pos = {144, 96, 32, 31},
 	color = 0xFFa0accc
@@ -235,6 +241,7 @@ rttr:AddTerrain{
 	edgePriority = 8,
 	kind = "land",
 	humidity = 30,
+	temperature = 10,
 	texture = texFile,
 	pos = {0, 144, 32, 31},
 	color = 0xFF88b15e
@@ -361,6 +368,7 @@ rttr:AddTerrain{
 	kind = "land",
 	property = "walkable",
 	humidity = 0,
+	temperature = 5,
 	texture = texFile,
 	pos = {48, 0, 32, 31},
 	color = 0xFF0070b0
diff --git a/libs/libGamedata/gameData/TerrainDesc.cpp b/libs/libGamedata/gameData/TerrainDesc.cpp
index 107b241..c787d5c 100644
--- a/libs/libGamedata/gameData/TerrainDesc.cpp
+++ b/libs/libGamedata/gameData/TerrainDesc.cpp
@@ -62,6 +62,19 @@ uint8_t getDefaultHumidity(TerrainKind kind)
     }
     throw GameDataError("Invalid terrain kind: " + helpers::toString(kind));
 }
+
+uint8_t getDefaultTemperature(TerrainKind kind)
+{
+    switch(kind)
+    {
+        case TerrainKind::Lava: return 100;
+        case TerrainKind::Snow: return 0;
+        case TerrainKind::Land:
+        case TerrainKind::Water:
+        case TerrainKind::Mountain: return 50;
+    }
+    throw GameDataError("Invalid terrain kind: " + helpers::toString(kind));
+}
 } // namespace
 
 TerrainDesc::TerrainDesc(CheckedLuaTable luaData, const WorldDescription& worldDesc)
@@ -96,6 +109,7 @@ TerrainDesc::TerrainDesc(CheckedLuaTable luaData, const WorldDescription& worldD
     else
         throw GameDataError("Invalid property '" + property + "'");
     humidity = luaData.getOrDefault("humidity", getDefaultHumidity(kind));
+    temperature = luaData.getOrDefault("temperature", getDefaultTemperature(kind));
     luaData.getOrThrow(texturePath, "texture");
     lua::validatePath(texturePath);
     posInTexture = luaData.getRectOrDefault("pos", Rect());
diff --git a/libs/libGamedata/gameData/TerrainDesc.h b/libs/libGamedata/gameData/TerrainDesc.h
index b9e5afd..3fabf3f 100644
--- a/libs/libGamedata/gameData/TerrainDesc.h
+++ b/libs/libGamedata/gameData/TerrainDesc.h
@@ -70,6 +70,11 @@ struct TerrainDesc
     ETerrain flags;
     /// How much water can be on the terrain [0, 100]
     uint8_t humidity;
+    /// How warm the terrain is [0, 100] - 0 = arctic/cold, 100 = tropical/hot. Independent of
+    /// humidity; used together with it by the map generator (rttr::mapGenerator::Texturizer) to
+    /// pick a terrain for a given point based on its local climate (humidity x temperature), similar
+    /// to a Whittaker biome diagram.
+    uint8_t temperature;
     ETexType texType;
     std::string texturePath;
     Rect posInTexture;
diff --git a/libs/s25main/mapGenerator/Map.cpp b/libs/s25main/mapGenerator/Map.cpp
index ae887ba..560a322 100644
--- a/libs/s25main/mapGenerator/Map.cpp
+++ b/libs/s25main/mapGenerator/Map.cpp
@@ -18,6 +18,7 @@ Map::Map(const MapExtent& size, uint8_t players, const WorldDescription& worldDe
 {
     z.Resize(size);
     climate.Resize(size, 50);
+    temperature.Resize(size, 50);
     objectTypes.Resize(size, libsiedler2::OT_Empty);
     objectInfos.Resize(size, libsiedler2::OI_Empty);
     resources.Resize(size, libsiedler2::R_None);
diff --git a/libs/s25main/mapGenerator/Map.h b/libs/s25main/mapGenerator/Map.h
index 918cde5..564939d 100644
--- a/libs/s25main/mapGenerator/Map.h
+++ b/libs/s25main/mapGenerator/Map.h
@@ -26,6 +26,9 @@ public:
     /// Local humidity [0, 100] per node, independent of z (height). Generated by GenerateClimateMap()
     /// and used by Texturizer to pick between land textures of the same height band.
     NodeMapBase<uint8_t> climate;
+    /// Local temperature [0, 100] per node, independent of z and climate. Generated by
+    /// GenerateClimateMap() with a different seed and used together with climate by Texturizer.
+    NodeMapBase<uint8_t> temperature;
     NodeMapBase<uint8_t> objectTypes;
     NodeMapBase<uint8_t> objectInfos;
     NodeMapBase<uint8_t> resources;
diff --git a/libs/s25main/mapGenerator/RandomMap.cpp b/libs/s25main/mapGenerator/RandomMap.cpp
index cf9d9ce..c660705 100644
--- a/libs/s25main/mapGenerator/RandomMap.cpp
+++ b/libs/s25main/mapGenerator/RandomMap.cpp
@@ -121,7 +121,7 @@ void SmoothHeightMap(NodeMapBase<uint8_t>& z, const ValueRange<uint8_t>& range)
 }
 
 RandomMap::RandomMap(RandomUtility& rnd, Map& map)
-    : rnd_(rnd), map_(map), texturizer_(map.z, map.climate, map.getTextures(), map.textureMap)
+    : rnd_(rnd), map_(map), texturizer_(map.z, map.climate, map.temperature, map.getTextures(), map.textureMap)
 {}
 
 void RandomMap::Create(const MapSettings& settings)
@@ -131,6 +131,7 @@ void RandomMap::Create(const MapSettings& settings)
     settings_ = settings;
     map_.z.Resize(settings.size, defaultHeight);
     map_.climate = GenerateClimateMap(rnd_, settings.size);
+    map_.temperature = GenerateClimateMap(rnd_, settings.size, 60);
 
     switch(settings.style)
     {
diff --git a/libs/s25main/mapGenerator/TextureHelper.cpp b/libs/s25main/mapGenerator/TextureHelper.cpp
index bdce06b..fcc212b 100644
--- a/libs/s25main/mapGenerator/TextureHelper.cpp
+++ b/libs/s25main/mapGenerator/TextureHelper.cpp
@@ -11,6 +11,11 @@ uint8_t ByHumidity(const TerrainDesc& terrain)
     return terrain.humidity;
 }
 
+uint8_t ByTemperature(const TerrainDesc& terrain)
+{
+    return terrain.temperature;
+}
+
 bool IsWater(const TerrainDesc& terrain)
 {
     return terrain.kind == TerrainKind::Water;
diff --git a/libs/s25main/mapGenerator/TextureHelper.h b/libs/s25main/mapGenerator/TextureHelper.h
index 29c7cf9..f85d134 100644
--- a/libs/s25main/mapGenerator/TextureHelper.h
+++ b/libs/s25main/mapGenerator/TextureHelper.h
@@ -10,6 +10,8 @@ namespace rttr::mapGenerator {
 
 uint8_t ByHumidity(const TerrainDesc& terrain);
 
+uint8_t ByTemperature(const TerrainDesc& terrain);
+
 bool IsWater(const TerrainDesc& terrain);
 
 bool IsSwamp(const TerrainDesc& terrain);
diff --git a/libs/s25main/mapGenerator/Textures.cpp b/libs/s25main/mapGenerator/Textures.cpp
index af4bb03..3696f90 100644
--- a/libs/s25main/mapGenerator/Textures.cpp
+++ b/libs/s25main/mapGenerator/Textures.cpp
@@ -7,6 +7,7 @@
 #include "mapGenerator/TextureHelper.h"
 
 #include <algorithm>
+#include <limits>
 #include <set>
 #include <stdexcept>
 
@@ -52,20 +53,19 @@ void Texturizer::ApplyTexturingByHeightMap(unsigned mountainLevel)
     }
 
     const uint8_t maxZ = *std::max_element(z_.begin(), z_.end());
-    const ValueRange<uint8_t> humidityRange(0, 100);
 
     const auto waterTexture = textureMap_.Find(IsShipableWater);
-    auto landTextures = textureMap_.FindAll(IsBuildableLand);
-    textureMap_.Sort(landTextures, ByHumidity);
+    const auto landTextures = textureMap_.FindAll(IsBuildableLand);
     auto mountainTextures = textureMap_.FindAll(IsMinableMountain);
     mountainTextures.push_back(textureMap_.Find(IsSnowOrLava));
 
     const MapExtent size = z_.GetSize();
     const auto& z = z_;
     const auto& climate = climate_;
+    const auto& temperature = temperature_;
 
     // Assumptions (same as before):
-    // 1) sum of 3 height/climate values is always < 256 (1 byte)
+    // 1) sum of 3 height/climate/temperature values is always < 256 (1 byte)
     // 2) we always want to round to the next integer (ceil)
     auto interpolate = [&size](const NodeMapBase<uint8_t>& values, const Triangle& triangle) {
         const auto& edges = GetTriangleEdges(triangle, size);
@@ -73,11 +73,33 @@ void Texturizer::ApplyTexturingByHeightMap(unsigned mountainLevel)
           std::ceil(static_cast<double>(values[edges[0]] + values[edges[1]] + values[edges[2]]) / 3));
     };
 
-    // Combines the height map and the climate map into a texture: the height decides *which band*
-    // a point falls into (water / buildable land / mountain), exactly as before. But *within* the
-    // land band, the texture is now picked by the local climate value instead of by height, so a
-    // desert region and a swamp region can appear at the same altitude. Mountain/snow/lava textures
-    // stay purely height-driven, same as before.
+    // Picks the buildable-land terrain whose (humidity, temperature) is closest (squared distance)
+    // to the given local climate/temperature reading - a simple 2D "biome diagram" lookup, e.g. a
+    // point that is both dry and hot ends up close to a desert/savanna terrain, while a point that
+    // is wet and warm ends up close to a swamp, regardless of terrains in between.
+    auto pickLandTexture = [&](uint8_t climateValue, uint8_t temperatureValue) {
+        DescIdx<TerrainDesc> best;
+        int bestDistance = std::numeric_limits<int>::max();
+        for(const auto& candidate : landTextures)
+        {
+            const TerrainDesc& desc = textureMap_.GetDesc(candidate);
+            const int dHumidity = static_cast<int>(desc.humidity) - climateValue;
+            const int dTemperature = static_cast<int>(desc.temperature) - temperatureValue;
+            const int distance = dHumidity * dHumidity + dTemperature * dTemperature;
+            if(distance < bestDistance)
+            {
+                bestDistance = distance;
+                best = candidate;
+            }
+        }
+        return best;
+    };
+
+    // Combines the height map with the climate/temperature maps into a texture: the height decides
+    // *which band* a point falls into (water / buildable land / mountain), exactly as before. But
+    // *within* the land band, the texture is now picked by local humidity and temperature instead of
+    // by height, so e.g. a desert region and a swamp region can appear at the same altitude.
+    // Mountain/snow/lava textures stay purely height-driven, same as before.
     auto pickTexture = [&](const Triangle& triangle) {
         const uint8_t zValue = interpolate(z, triangle);
 
@@ -88,7 +110,8 @@ void Texturizer::ApplyTexturingByHeightMap(unsigned mountainLevel)
         if(zValue < mountainLevel)
         {
             const uint8_t climateValue = interpolate(climate, triangle);
-            return landTextures[MapValueToIndex(climateValue, humidityRange, landTextures.size())];
+            const uint8_t temperatureValue = interpolate(temperature, triangle);
+            return pickLandTexture(climateValue, temperatureValue);
         }
 
         const ValueRange<uint8_t> mountainRange(mountainLevel, maxZ);
diff --git a/libs/s25main/mapGenerator/Textures.h b/libs/s25main/mapGenerator/Textures.h
index 5fe1991..68eb72b 100644
--- a/libs/s25main/mapGenerator/Textures.h
+++ b/libs/s25main/mapGenerator/Textures.h
@@ -39,6 +39,10 @@ public:
 
     uint8_t GetLandscapeId() const { return worldDesc_.get(landscape_).s2Id; }
 
+    /// Returns the full terrain description (humidity, temperature, ...) for a candidate texture -
+    /// useful when picking among several candidates by more than one property at once.
+    const TerrainDesc& GetDesc(DescIdx<TerrainDesc> texture) const { return worldDesc_.get(texture); }
+
     template<class T_Predicate>
     DescIdx<TerrainDesc> Find(T_Predicate predicate) const
     {
@@ -148,19 +152,23 @@ class Texturizer
 {
 private:
     NodeMapBase<uint8_t>& z_;
-    /// Local humidity [0, 100] per node (see Map::climate), independent of z_. Used to pick which
-    /// land texture (of several at the same height) is placed at a given point - see
-    /// ApplyTexturingByHeightMap.
+    /// Local humidity [0, 100] per node (see Map::climate), independent of z_.
     NodeMapBase<uint8_t>& climate_;
+    /// Local temperature [0, 100] per node (see Map::temperature), independent of z_ and climate_.
+    /// Together, climate_ and temperature_ form a 2D "biome space" (like a Whittaker diagram) used
+    /// to pick which land texture is placed at a given point - see ApplyTexturingByHeightMap.
+    NodeMapBase<uint8_t>& temperature_;
     NodeMapBase<TexturePair>& textures_;
     TextureMap& textureMap_;
     unsigned seaLevel_;
 
     /**
      * Adds textures based on z-values, sea- and mountain levels. Land textures within the
-     * buildable band (between sea level and mountainLevel) are chosen by climate_ rather than by
-     * z_ alone, so two points at the same height can get different biomes depending on their local
-     * climate value. Mountain/snow/lava textures above mountainLevel remain purely height-driven.
+     * buildable band (between sea level and mountainLevel) are chosen by matching climate_ and
+     * temperature_ against each candidate terrain's (humidity, temperature) pair - the closest
+     * match wins - rather than by z_ alone, so two points at the same height can get different
+     * biomes depending on their local climate. Mountain/snow/lava textures above mountainLevel
+     * remain purely height-driven.
      *
      * @param mountainLevel minimum height mountains start from
      */
@@ -189,9 +197,9 @@ private:
     void ApplyMountainWaterTransitions(const std::vector<MapPoint>& transitions);
 
 public:
-    Texturizer(NodeMapBase<uint8_t>& z, NodeMapBase<uint8_t>& climate, NodeMapBase<TexturePair>& textures,
-               TextureMap& textureMap)
-        : z_(z), climate_(climate), textures_(textures), textureMap_(textureMap)
+    Texturizer(NodeMapBase<uint8_t>& z, NodeMapBase<uint8_t>& climate, NodeMapBase<uint8_t>& temperature,
+               NodeMapBase<TexturePair>& textures, TextureMap& textureMap)
+        : z_(z), climate_(climate), temperature_(temperature), textures_(textures), textureMap_(textureMap)
     {
         seaLevel_ = GetRange(z).minimum;
     }
diff --git a/tests/s25Main/mapGenerator/testTextures.cpp b/tests/s25Main/mapGenerator/testTextures.cpp
index edae766..8711063 100644
--- a/tests/s25Main/mapGenerator/testTextures.cpp
+++ b/tests/s25Main/mapGenerator/testTextures.cpp
@@ -28,11 +28,13 @@ class TextureMapFixtureWithZ : public TextureMapFixture
 public:
     NodeMapBase<uint8_t> z;
     NodeMapBase<uint8_t> climate;
+    NodeMapBase<uint8_t> temperature;
 
     TextureMapFixtureWithZ()
     {
         z.Resize(textures.GetSize());
         climate.Resize(textures.GetSize(), 50);
+        temperature.Resize(textures.GetSize(), 50);
     }
 };
 
@@ -48,7 +50,7 @@ BOOST_FIXTURE_TEST_CASE(AddTextures_sets_valid_textures_for_entire_map, TextureM
         z[pt] = pt.x % 10;
     }
 
-    Texturizer texturizer(z, climate, textures, textureMap);
+    Texturizer texturizer(z, climate, temperature, textures, textureMap);
     texturizer.AddTextures(mountainLevel, coastline);
 
     RTTR_FOREACH_PT(MapPoint, textures.GetSize())
@@ -70,7 +72,7 @@ BOOST_FIXTURE_TEST_CASE(AddTextures_does_not_override_textures, TextureMapFixtur
         z[pt] = static_cast<uint8_t>(pt.x % 256);
     }
 
-    Texturizer texturizer(z, climate, textures, textureMap);
+    Texturizer texturizer(z, climate, temperature, textures, textureMap);
     texturizer.AddTextures(mountainLevel, coastline);
 
     RTTR_FOREACH_PT(MapPoint, textures.GetSize())
@@ -85,7 +87,7 @@ BOOST_FIXTURE_TEST_CASE(AddTextures_sets_water_textures_for_minimum_height, Text
     const unsigned mountainLevel = 3;
     const unsigned coastline = 2;
 
-    Texturizer texturizer(z, climate, textures, textureMap);
+    Texturizer texturizer(z, climate, temperature, textures, textureMap);
     texturizer.AddTextures(mountainLevel, coastline);
 
     RTTR_FOREACH_PT(MapPoint, textures.GetSize())
@@ -102,7 +104,7 @@ BOOST_FIXTURE_TEST_CASE(AddTextures_sets_mountain_textures_above_mountain_level,
     z.Resize(z.GetSize(), mountainLevel);
     z[0] = 1; // sea
 
-    Texturizer texturizer(z, climate, textures, textureMap);
+    Texturizer texturizer(z, climate, temperature, textures, textureMap);
     texturizer.AddTextures(mountainLevel, coastline);
 
     RTTR_FOREACH_PT(MapPoint, textures.GetSize())
